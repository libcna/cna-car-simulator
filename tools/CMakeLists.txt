# Command-line tools (map validation, plate generation checks, ...). Populated
# as the corresponding subsystems land; see plan.md.
add_subdirectory(simtrace)
add_subdirectory(mapvalidate)
add_subdirectory(audio_preview)
